meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands:
    - npm run build
  artifacts:
    "*": /
run:
  command: npm run server
  persistenceMount: /data
  containerPort: "3001"
  servicePort: "80"
